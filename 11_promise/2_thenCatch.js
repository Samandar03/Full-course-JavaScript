const myPromise = new Promise((resolve,reject)=>{
    // Выпалнение 

     // Резултат
});

myPromise 
.then(value =>{
    // Действе

    // Значениея resolve
})

.catch(error =>{
    //Действе

    //Значениея rejects
})